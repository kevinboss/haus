namespace Haus.Commands.Helper;

internal static class HelperCommands
{
    public static string Create(string domain) => $"{domain}/create";
    public static string Delete(string domain) => $"{domain}/delete";
}
